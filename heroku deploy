1. heroku login
2. heroku create
3. git push heroku master
4. heroku ps:scale web=1
5. heroku open


Procfile: declares wht command should be executed to start your app
requirements.txt: Heroku will recognise your app as a python app if you have one


1. git add .
2. git commit -m "Message" 
3. git push heroku master
4. heroku open






1. Create a repository
2. Navigate to that folder
3. Make a file named app.py
4. Make a file named Procfile
5. Make a file named requirements.txt
6. Make a file named Procfile
7. Make a file named setup.sh

8. git add .
   git commit -m "Some message"
   git push heroku master



































